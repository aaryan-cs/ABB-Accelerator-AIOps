# agents (P5)
LangGraph wiring for A1-A5 + the language layer. Engine math lives in ../correlation (13/13 tests green).
Start here after P3 freezes the event schema: consume GET /window, emit Verdict/Insight JSON.
